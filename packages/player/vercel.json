{
  "buildCommand": "bun run build",
  "installCommand": "bun install",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/player/(.*)",
      "destination": "/api/index"
    }
  ]
}